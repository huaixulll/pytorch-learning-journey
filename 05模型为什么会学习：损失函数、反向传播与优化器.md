# 模型为什么会学习：损失函数、反向传播与优化器

训练网络时有三个连续的问题：模型预测得有多差？怎样知道每个参数该往哪个方向改？又怎样真的改动参数？对应到 PyTorch 中，就是 **损失函数（loss）—反向传播（backward）—优化器（optimizer）**。



这一篇基于三个练习展开：比较常用 loss、让 CNN 执行一次反向传播、再让 SGD 连续更新参数。



> 注意：下文构造函数应写为 `__init__`，即前后各两个下划线。聊天中的 `**init**` 是 Markdown 加粗造成的显示问题，不能直接复制到 Python 文件中。
> 
> 



## 1\. 损失函数：先量化“预测错了多少”



损失函数接收模型输出和目标值，返回一个数值。训练时，这个数值既用于判断模型表现，也为后续的参数更新提供依据。



### 1\.1 L1Loss 与 MSELoss



下面的例子中，预测值是 `[1, 2, 3]`，目标值是 `[1, 2, 5]`，只有最后一个位置相差 2。



```Python
from torch import nn
import torch

inputs = torch.tensor([1, 2, 3], dtype=torch.float32)
targets = torch.tensor([1, 2, 5], dtype=torch.float32)

# 为了练习，先改成四维形状；这不是 L1/MSE 的必要条件
inputs = torch.reshape(inputs, (1, 1, 1, 3))
targets = torch.reshape(targets, (1, 1, 1, 3))
```



形状变为 `[1, 1, 1, 3]` 后，两个张量仍有 3 个元素，数值关系没有改变。



```Python
# L1：所有绝对误差求和
loss_l1 = nn.L1Loss(reduction="sum")
result_l1 = loss_l1(inputs, targets)
print(result_l1)  # tensor(2.)

# MSE：平方误差的平均值
loss_mse = nn.MSELoss()
result_mse = loss_mse(inputs, targets)
print(result_mse)  # tensor(1.3333)
```



- `L1Loss(reduction="sum")` 计算 `|1-1| + |2-2| + |3-5| = 2`。

- `MSELoss()` 默认取平均：`(0² + 0² + (-2)²) / 3 = 4/3`。平方会更严厉地惩罚较大的误差。

    

这类 loss 常用于回归问题，例如预测房价、温度或连续数值。



### 1\.2 CrossEntropyLoss：分类任务的常用损失



CIFAR\-10 是十分类问题，通常使用 `CrossEntropyLoss`。它需要两种输入：



- 模型输出：shape 为 `[N, C]` 的 logits，`N` 是样本数，`C` 是类别数；

- 真实标签：shape 为 `[N]` 的类别编号，取值范围为 `0` 到 `C-1`。



注释：

`logits` 是模型在最后一层输出的“原始类别得分”，还不是概率。



```Python
x = torch.tensor([0.1, 0.2, 0.3])  # 三个类别的 logits
y = torch.tensor([1])              # 正确类别编号是 1

x = torch.reshape(x, (1, 3))       # [N=1, C=3]
loss_cross = nn.CrossEntropyLoss()
result_cross = loss_cross(x, y)
print(result_cross)  # 约为 tensor(1.1019)
```



`CrossEntropyLoss` 内部已经完成了 Softmax 和对数计算，因此输入应是原始 logits；不要在网络输出后再手动加 `Softmax`。



例子：

![image\.png](图片和附件/image%202.png)

![image\.png](图片和附件/image.png)

![image\.png](图片和附件/image%201.png)

注：log实际是ln函数



## 2\. 反向传播：从 loss 计算每个参数的梯度



损失告诉我们“错了多少”，但还没有告诉每一个卷积核和全连接层参数应该如何调整。`result_loss.backward()` 会沿着网络反向计算梯度，并将结果累积到各参数的 `.grad` 中。



### 2\.1 定义用于练习的 CNN



下面的 `TestLossNetwork` 与前一篇的完整 CNN 结构一致：三段卷积和池化提取特征，`Flatten` 将 `[64, 4, 4]` 展平为 1024 个特征，最后输出 10 个类别得分。



```Python
import torchvision
from torch import nn
from torch.utils.data import DataLoader

dataset = torchvision.datasets.CIFAR10(
    "./data", train=False,
    transform=torchvision.transforms.ToTensor(), download=True
)
dataloader = DataLoader(dataset, batch_size=1)

class TestLossNetwork(nn.Module):
    def __init__(self):
        super().__init__()
        self.model1 = nn.Sequential(
            nn.Conv2d(3, 32, 5, padding=2),
            nn.MaxPool2d(2),
            nn.Conv2d(32, 32, 5, padding=2),
            nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 5, padding=2),
            nn.MaxPool2d(2),
            nn.Flatten(),
            nn.Linear(1024, 64),
            nn.Linear(64, 10),
        )

    def forward(self, input):
        return self.model1(input)
```



这里的 `batch_size=1` 只是为了让这次实验容易查看：输入图片 shape 是 `[1, 3, 32, 32]`，网络输出则是 `[1, 10]`。



### 2\.2 前向传播、计算 loss、执行 `backward()`



```Python
loss = nn.CrossEntropyLoss()
tln = TestLossNetwork()

for data in dataloader:
    imgs, targets = data

    # 1. 前向传播：每张图得到 10 个类别得分
    outputs = tln(imgs)
    print(outputs)
    print(targets)

    # 2. 将输出与真实类别编号交给交叉熵损失
    result_loss = loss(outputs, targets)

    # 3. 反向传播：计算网络参数的梯度
    result_loss.backward()
    break
```



这一段只计算梯度，**还没有更新参数**。如果想查看某层的梯度，可以在 `backward()` 后打印，例如 `tln.model1[0].weight.grad`。在完整训练中，梯度会交给优化器用于更新参数。



## 3\. 优化器：根据梯度实际更新参数



以 SGD 为例，优化器在每次调用 `step()` 时大致执行：



```Plain Text
新参数 = 旧参数 - 学习率 × 梯度
```



学习率就是每次调整的步幅。步幅太大可能让 loss 震荡，太小则会让训练很慢。



### 3\.1 创建模型、loss 与 SGD



```Python
import torch

to = TestOptim()
loss = nn.CrossEntropyLoss()

# 参数来自模型；学习率是 0.01
optim = torch.optim.SGD(to.parameters(), lr=0.01)
```



`to.parameters()` 将网络中所有可训练参数交给 SGD 管理。此处变量名 `to` 只是模型实例名，并不是 PyTorch 的 `.to(device)` 方法。



### 3\.2 完整的更新顺序



下面是训练循环，并按每步作用拆开说明。DataLoader 没有显式传入 `batch_size`，所以默认一次读取 1 张图片；外层总共训练 20 轮。



```Python
for epoch in range(20):
    running_loss = 0.0

    for data in dataloader:
        imgs, targets = data

        # 1. 前向传播：模型给出类别得分
        outputs = to(imgs)

        # 2. 计算预测结果与真实标签的差距
        result_loss = loss(outputs, targets)

        # 3. 清空上一批数据残留的梯度
        optim.zero_grad()

        # 4. 从 loss 反向计算每个参数的梯度
        result_loss.backward()

        # 5. 按 SGD 规则更新参数
        optim.step()

        # 6. 累加本轮 loss，用于观察训练过程
        running_loss += result_loss.item()

    print(running_loss)
```



原练习写作 `running_loss = running_loss + result_loss` 也能打印结果，但会不断累积 Tensor 及其计算关系。用于统计和打印时，改为 `result_loss.item()` 更合适：它只保留普通数值，不再保存反向传播所需的计算图。



最容易遗忘的顺序是：



```Python
optim.zero_grad()
result_loss.backward()
optim.step()
```



`zero_grad()` 必须放在 `backward()` 前面，因为 PyTorch 默认会累积梯度。如果跳过 `step()`，模型参数不会改变；如果跳过 `zero_grad()`，当前 batch 的梯度会叠加到之前的梯度上。



## 4\. 三者如何连起来



```Plain Text
图片 imgs + 标签 targets
  -> 模型输出 logits
  -> CrossEntropyLoss 得到 result_loss
  -> result_loss.backward() 计算每个参数的 grad
  -> optim.step() 根据 grad 更新参数
  -> 下一批数据，重复上述过程
```



对于 CIFAR\-10，网络最终输出 shape 是 `[N, 10]`，标签 shape 是 `[N]`。只要这两个 shape 对得上，就能把它们直接传入 `CrossEntropyLoss`。



## 本篇小任务



1. 先运行 L1、MSE、交叉熵的三个小例子，确认理解它们各自衡量的误差。

2. 在 `result_loss.backward()` 后打印第一层卷积权重的 `.grad`，观察梯度已经生成。

3. 在训练循环里暂时注释 `optim.step()`，观察 running loss 的变化；再恢复它，体会“有梯度”和“参数真正更新”是两件不同的事。

