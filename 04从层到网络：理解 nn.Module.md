# 从层到网络：理解 nn\.Module

`nn.Module` 是 PyTorch 中所有神经网络的基类。它不只是把若干层放在一起，更负责管理网络参数、记录子模块，并让我们可以用统一的 `model(input)` 写法完成前向传播。



这一篇按照我的实际练习顺序，从一个“输入加一”的最小网络开始，逐步走到完整的 CIFAR\-10 CNN。



> 注意：下文代码中的构造函数必须写成 `__init__`，即前后各两个下划线。聊天中显示的 `**init**` 是 Markdown 加粗造成的显示问题，不能直接复制到 Python 文件中。
> 
> 



## 1\. 最小实验：`nn.Module` 与 `forward`



先不引入卷积或数据集，只让网络把输入加一。这个例子用来理解 `__init__` 和 `forward` 的分工。



```Python
import torch
from torch import nn

class Huai(nn.Module):
    def __init__(self):
        super().__init__()

    def forward(self, input):
        output = input + 1
        return output

huaixu = Huai()
x = torch.tensor(1.0)
output = huaixu(x)
print(output)  # tensor(2.)
```



- `__init__`：定义网络需要哪些层或参数。这个最小例子没有可学习层，所以只调用父类初始化。

- `forward`：定义输入如何变成输出；这里就是 `input + 1`。

- `huaixu(x)`：推荐的模型调用方式。PyTorch 会自动执行 `forward(x)`，通常不直接写 `huaixu.forward(x)`。

    

## 2\. 先用 `F.conv2d` 看卷积在做什么



感兴趣的可以查看链接中的动画演示：
https://github\.com/vdumoulin/conv\_arithmetic/blob/master/README\.md



在使用 `nn.Conv2d` 前，我先用一个 5×5 输入和 3×3 卷积核调用 `torch.nn.functional.conv2d`。这个实验没有可学习参数，重点是观察 stride 和 padding 如何改变输出形状。



### 2\.1 把二维矩阵改为卷积需要的四维形状



`conv2d` 的输入形状是 `[N, C, H, W]`，分别是 batch 大小、通道数、高和宽；卷积核形状是 `[输出通道数, 输入通道数, 核高, 核宽]`。



```Python
import torch
import torch.nn.functional as F

input = torch.tensor([[1, 2, 0, 3, 1],
                      [0, 1, 2, 3, 1],
                      [1, 2, 1, 0, 0],
                      [5, 2, 3, 1, 1],
                      [2, 1, 0, 1, 1]])

kernel = torch.tensor([[1, 2, 1],
                       [0, 1, 0],
                       [2, 1, 0]])

input = torch.reshape(input, (1, 1, 5, 5))
kernel = torch.reshape(kernel, (1, 1, 3, 3))

print(input.shape)   # torch.Size([1, 1, 5, 5])
print(kernel.shape)  # torch.Size([1, 1, 3, 3])
```



这里的两个 `1` 分别表示“只输入一张数据”和“只有一个通道”。



### 2\.2 比较 stride 与 padding



```Python
output = F.conv2d(input, kernel, stride=1)
print(output)        # 输出空间大小：3 × 3

output2 = F.conv2d(input, kernel, stride=2)
print(output2)       # 步长变大，输出空间大小：2 × 2

output3 = F.conv2d(input, kernel, stride=1, padding=1)
print(output3)       # 四周补零，输出空间大小恢复为 5 × 5
```



- `stride=1`：卷积核每次移动 1 格，5×5 输入经过 3×3 卷积核后得到 3×3 输出。

- `stride=2`：卷积核每次移动 2 格，输出更小。

- `padding=1`：在输入周围补一圈 0，使 3×3 卷积核能够覆盖边缘区域；此例输出高宽与原输入相同。

    

## 3\. `nn.Conv2d`：把卷积用在 CIFAR\-10 图片上



`F.conv2d` 适合帮助理解一次卷积计算；在网络里通常使用 `nn.Conv2d`，因为它会把卷积核注册为可训练参数。



### 3\.1 定义一层卷积网络



```Python
import torch
import torchvision
from torch import nn
from torch.nn import Conv2d
from torch.utils.data import DataLoader
from torch.utils.tensorboard import SummaryWriter

dataset = torchvision.datasets.CIFAR10(
    "./data", train=False,
    transform=torchvision.transforms.ToTensor(), download=True
)
dataloader = DataLoader(dataset, batch_size=64)

class Huai(nn.Module):
    def __init__(self):
        super().__init__()
        self.conv1 = Conv2d(
            in_channels=3,
            out_channels=6,
            kernel_size=3,
            stride=1,
            padding=0,
        )

    def forward(self, x):
        return self.conv1(x)
```



这里输入的 CIFAR\-10 图片是 RGB 三通道，因此 `in_channels=3`；卷积层提取 6 组特征，因此 `out_channels=6`。尺寸计算为 `(32 - 3 + 2 × 0) / 1 + 1 = 30`，所以输出 shape 是 `[64, 6, 30, 30]`。

注释：尺寸计算可以由下述公式推导，若嫌麻烦，直接问AI即可

![image\.png](图片和附件/image%201.png)



### 3\.2 在 TensorBoard 中查看输入与卷积输出



```Python
huaixu = Huai()
writer = SummaryWriter("./logs")
step = 0

for data in dataloader:
    imgs, targets = data
    output = huaixu(imgs)

    print(imgs.shape)    # torch.Size([64, 3, 32, 32])
    print(output.shape)  # torch.Size([64, 6, 30, 30])

    writer.add_images("input", imgs, step)

    # 仅为在 TensorBoard 中展示：把 6 个特征通道两两拼为 3 通道图像
    display_output = torch.reshape(output, (-1, 3, 30, 30))
    writer.add_images("output", display_output, step)
    step += 1

writer.close()
```



`output` 有 6 个特征通道，不能直接当作普通 RGB 图片理解。这里 reshape 成 `[-1, 3, 30, 30]` 的目的只是为了可视化：它会把每 3 个特征图临时当作一张三通道图片。这个 reshape **不应该放进正式模型的计算流程**，也不代表卷积输出真的变成了彩色图片。



## 4\. `MaxPool2d`：保留局部最大值并缩小特征图



最大池化会在一个局部窗口内选择最大的数。它能减小高宽、减少后续计算量，并保留较强的局部响应。



```Python
import torch
import torchvision
from torch import nn
from torch.nn import MaxPool2d
from torch.utils.data import DataLoader
from torch.utils.tensorboard import SummaryWriter

dataset = torchvision.datasets.CIFAR10(
    root="./data", train=False, download=True,
    transform=torchvision.transforms.ToTensor()
)
dataloader = DataLoader(dataset, batch_size=64)

class Huai(nn.Module):
    def __init__(self):
        super().__init__()
        self.maxpool1 = MaxPool2d(kernel_size=3, ceil_mode=False)

    def forward(self, input):
        return self.maxpool1(input)
```



你的代码没有显式设置 stride，`MaxPool2d` 默认使用 `stride=kernel_size=3`。因此对 `[64, 3, 32, 32]` 的输入，输出大小为 `[64, 3, 10, 10]`。`ceil_mode=False` 表示输出尺寸采用向下取整。



下面把池化前后的图片写入 TensorBoard，观察尺寸的变化：



```Python
huaixu = Huai()
writer = SummaryWriter("logs_maxpool")
step = 0

for data in dataloader:
    imgs, targets = data
    writer.add_images("input", imgs, step)

    output = huaixu(imgs)
    writer.add_images("output", output, step)
    step += 1

writer.close()
```



池化不改变通道数，所以输入和输出都还是 3 通道，能直接用 `add_images()` 显示，显示结果如下：

![image\.png](图片和附件/image%202.png)

![image\.png](图片和附件/image%203.png)



## 5\. 激活函数：代码实际运行的是 Sigmoid



激活函数为网络加入非线性。没有激活函数时，多层线性层叠加后仍然等价于一个线性变换。



```Python
class Huaixu(nn.Module):
    def __init__(self):
        super().__init__()
        self.relu1 = nn.ReLU()
        self.sigmoid1 = nn.Sigmoid()

    def forward(self, input):
        output = self.sigmoid1(input)
        # 如果改为下面一行，才是在运行 ReLU：
        # output = self.relu1(input)
        return output
```



演示的是 **Sigmoid**：它将数值压缩到 `(0, 1)`。若改用 `ReLU`，所有负数会变为 0，正数保持不变。



在 CIFAR\-10 数据上把输入和输出写入 TensorBoard：



```Python
writer = SummaryWriter("logs_sigmoid")
step = 0

for data in dataloader:
    imgs, targets = data
    writer.add_images("input", imgs, global_step=step)

    output = huaixu(imgs)
    writer.add_images("output", output, step)
    step += 1

writer.close()
```

查看运行结果如下：

![image\.png](图片和附件/image.png)

![image\.png](图片和附件/image%204.png)



## 6\. `Linear` 与 Flatten：先看清 batch 维度



全连接层 `Linear(in_features, out_features)` 接收的最后一维大小必须等于 `in_features`。练习将一个 batch 的所有元素都展平：



```Python
imgs, target = next(iter(dataLoader))
print(imgs.shape)  # torch.Size([64, 3, 32, 32])

output = torch.flatten(imgs)
print(output.shape)  # torch.Size([196608])，即 64 × 3 × 32 × 32
```



因此 `Linear(196608, 10)` 能将这个一维向量映射为 10 个数，但它会把 **64 张图片混成一个样本**。这适合作为理解 `flatten` 的小实验，却不适合真正的分类训练。



在正式模型中，应保留 batch 维度，只展平每张图片的特征：



```Python
imgs, target = next(iter(dataLoader))
features = torch.flatten(imgs, start_dim=1)
print(features.shape)  # torch.Size([64, 3072])

linear = nn.Linear(3 * 32 * 32, 10)
logits = linear(features)
print(logits.shape)    # torch.Size([64, 10])
```



这就是为什么完整 CNN 中使用 `nn.Flatten()`：默认会从第 1 维开始展平，保留第 0 维的 batch 大小。



## 7\. `nn.Sequential`：把层按顺序组装成完整 CNN



前面的卷积、池化、展平和全连接层可以逐行写在 `forward` 中，也可以用 `nn.Sequential` 将严格按顺序执行的层组成一个模块。对于当前的 CNN，`Sequential` 更紧凑。



```Python
import torch
from torch import nn
from torch.utils.tensorboard import SummaryWriter

class Huai(nn.Module):
    def __init__(self):
        super().__init__()
        self.model = nn.Sequential(
            nn.Conv2d(3, 32, 5, padding=2),
            nn.MaxPool2d(2),
            nn.Conv2d(32, 32, 5, padding=2),
            nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 5, padding=2),
            nn.MaxPool2d(2),
            nn.Flatten(),
            nn.Linear(1024, 64),  # 1024 = 64 × 4 × 4
            nn.Linear(64, 10),
        )

    def forward(self, input):
        return self.model(input)
```



### 逐层追踪 shape



对于输入 `[64, 3, 32, 32]`，每层输出如下：



|层|输出 shape|
|---|---|
|Conv2d\(3, 32, 5, padding=2\)|`[64, 32, 32, 32]`|
|MaxPool2d\(2\)|`[64, 32, 16, 16]`|
|Conv2d\(32, 32, 5, padding=2\)|`[64, 32, 16, 16]`|
|MaxPool2d\(2\)|`[64, 32, 8, 8]`|
|Conv2d\(32, 64, 5, padding=2\)|`[64, 64, 8, 8]`|
|MaxPool2d\(2\)|`[64, 64, 4, 4]`|
|Flatten\(\)|`[64, 1024]`|
|Linear\(1024, 64\)|`[64, 64]`|
|Linear\(64, 10\)|`[64, 10]`|



最后的 10 个数是每个类别的 logits，不是概率。它们可以直接交给 `CrossEntropyLoss`。



### 用随机输入和 TensorBoard 检查网络



```Python
huaixu = Huai()
print(huaixu)

input_data = torch.ones((64, 3, 32, 32))
output = huaixu(input_data)
print(output.shape)  # torch.Size([64, 10])

writer = SummaryWriter("logs_seq")
writer.add_graph(huaixu, input_data)
writer.close()
```



`print(huaixu)` 用来检查层的连接顺序；`print(output.shape)` 用来确认模型最后确实给每张图片输出 10 个类别得分；`add_graph()` 则把网络结构写入 TensorBoard，方便从整体检查模型。



## 本篇小结



```Plain Text
nn.Module：定义一个可以被调用的模型
  -> Conv2d：从图片提取局部特征
  -> MaxPool2d：缩小特征图并保留较强响应
  -> ReLU / Sigmoid：引入非线性
  -> Flatten：保留 batch 维度，展平每张图片的特征
  -> Linear：将特征映射为 10 个类别得分
  -> Sequential：把顺序执行的层组装为一个完整网络
```



下一步就是用 `CrossEntropyLoss` 衡量这 10 个类别得分与真实标签之间的差距，再通过反向传播和优化器更新这些层中的参数。

